# Comum

Arquivos compartilhados entre todos os módulos.

- **config.js** — Hosts, endpoints, filas, timeout. Edite aqui para produção.
- **loadings.js** — Detecção de loadings do PJe (RichFaces, Angular, etc.)
- **network-monitor.js** — Monitor de requisições XHR (debug)
